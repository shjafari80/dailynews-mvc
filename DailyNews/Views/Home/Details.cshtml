@model DailyNews.Models.News

@{
    ViewData["Title"] = Model.Title;
}

<h1 class="mb-3">@Model.Title</h1>

<div class="text-muted mb-4">
    <span>@Model.Category.Name</span> &bull;
    <span>by @Model.Author.Name</span> &bull;
    <span>@Model.PublishDate.ToString("dd MMMM yyyy - HH:mm")</span>
</div>

<div class="lead">
    @Model.Content
</div>

<a asp-action="Index" class="btn btn-secondary mt-4">Back to Home</a>
